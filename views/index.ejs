<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Artwork Finder</title>
    <link rel="stylesheet" type="text/css" href="styles/main.css">
</head>
<body>
    <div class="container">
        <h1>Artwork Finder</h1>
        <form method="post" action="/get-art">
            <label for="idInput">Enter Artwork ID:</label>
            <input type="text" id="idInput" name="id" required>
            <button type="submit">Search</button>
        </form>


        <% if (locals.objectID) { %>
            <h2>Title: <%= title %></h2>
            <% if (primaryImage) { %>
                <img src="<%= primaryImage %>" alt="<%= title %>" style="max-width: 100%; height: auto;">
            <% } else { %>
                <p>No image available for this artwork.</p>
            <% } %>
            <p>Object ID: <%= objectID %></p>
        <% } %>
    </div>
</body>
</html>